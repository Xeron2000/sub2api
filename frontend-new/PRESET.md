# Preset Source

- Preset: `b7WQfDSML` via `pnpm dlx shadcn@latest init --preset b7WQfDSML --template start`
- Date: 2026-08-26
- Style: `base-sera`, baseColor `taupe`, icon `remixicon`, menu inverted
- If `npx shadcn apply b7WQfDSML` fails on fresh install, this file + styles.css is the visual baseline; do not silently fallback.
